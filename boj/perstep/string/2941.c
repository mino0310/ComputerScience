#include <stdio.h>
#include <string.h>

int main(void)
{
	int len, cnt, chk[130] = {0, };
	char input[101];
	len = strlen(input);
	for (int i = 0; i < len; i++)
	{
		if (inputd 	[i] = 'd' && input[i+1] == 'z' && input[i+2] = '=')
		{
			chk[8]++;
		}
		else if (input[i+1] == '=' || input [i+1] == '-' || input[i+1] == 'j')
		{
			
		}
		else
		{

		}

	}
	return 0;
}
