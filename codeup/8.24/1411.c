#include <stdio.h>
int main(void)
{
	int n, m, arr[51], check = 0;
	scanf("%d", &n);
	for (int i = 0; i < n - 1; i++)
	{
		scanf("%d", &arr[i]);
	}

	f
	for (int i = 0; arr[i] != '\0'; i++)
	{
		printf("%d", arr[i]);
	}
	return 0;
}
